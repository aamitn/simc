---
---

<nav class="fixed top-0 left-0 right-0 z-50 bg-dark-bg/80 dark:bg-dark-bg/80 bg-light-bg/80 backdrop-blur-md border-b border-white/10 dark:border-white/10 border-gray-200 transition-colors duration-300" id="navbar">
  <div class="container mx-auto px-6 py-4">
    <div class="flex items-center justify-between">
      <!-- Logo -->
      <div class="flex items-center space-x-2" id="logo">
        <div class="w-8 h-8 bg-gradient-to-r from-circuit-blue to-circuit-purple rounded-lg flex items-center justify-center">
          <i data-feather="zap" class="w-5 h-5 text-white"></i>
        </div>
        <span class="text-xl font-bold text-white dark:text-white text-text-primary-light">CircuitSim</span>
      </div>
      
      <!-- Desktop Navigation -->
      <div class="hidden md:flex items-center space-x-8" id="nav-links">
        <a href="#home" class="nav-link text-text-secondary dark:text-text-secondary text-text-secondary-light hover:text-circuit-blue transition-colors">Home</a>
        <a href="#features" class="nav-link text-text-secondary dark:text-text-secondary text-text-secondary-light hover:text-circuit-blue transition-colors">Features</a>
        <a href="#about" class="nav-link text-text-secondary dark:text-text-secondary text-text-secondary-light hover:text-circuit-blue transition-colors">About</a>
        <a href="#contact" class="nav-link text-text-secondary dark:text-text-secondary text-text-secondary-light hover:text-circuit-blue transition-colors">Contact</a>
      </div>
      
      <!-- Theme Toggle -->
      <button class="hidden md:block w-10 h-10 bg-dark-secondary dark:bg-dark-secondary bg-light-secondary border border-white/10 dark:border-white/10 border-gray-300 rounded-lg flex items-center justify-center hover:border-circuit-blue/50 transition-all duration-300" id="theme-toggle" aria-label="Toggle theme">
        <i data-feather="sun" class="w-5 h-5 text-circuit-blue block dark:hidden"></i>
        <i data-feather="moon" class="w-5 h-5 text-circuit-blue hidden dark:block"></i>
      </button>
      
      <!-- CTA Button -->
      <button class="hidden md:block px-6 py-2 bg-gradient-to-r from-circuit-blue to-circuit-purple text-white font-semibold rounded-lg hover:shadow-lg hover:shadow-circuit-blue/25 transition-all duration-300" id="nav-cta">
        Launch Now
      </button>
      
      <!-- Mobile Menu Button -->
      <button class="md:hidden text-white" id="mobile-menu-btn">
        <i data-feather="menu" class="w-6 h-6"></i>
      </button>
    </div>
    
    <!-- Mobile Navigation -->
    <div class="md:hidden mt-4 pb-4 hidden" id="mobile-menu">
      <div class="flex flex-col space-y-4">
        <a href="#home" class="text-text-secondary dark:text-text-secondary text-text-secondary-light hover:text-circuit-blue transition-colors">Home</a>
        <a href="#features" class="text-text-secondary dark:text-text-secondary text-text-secondary-light hover:text-circuit-blue transition-colors">Features</a>
        <a href="#about" class="text-text-secondary dark:text-text-secondary text-text-secondary-light hover:text-circuit-blue transition-colors">About</a>
        <a href="#contact" class="text-text-secondary dark:text-text-secondary text-text-secondary-light hover:text-circuit-blue transition-colors">Contact</a>
        
        <!-- Mobile Theme Toggle -->
        <button class="w-10 h-10 bg-dark-secondary dark:bg-dark-secondary bg-light-secondary border border-white/10 dark:border-white/10 border-gray-300 rounded-lg flex items-center justify-center hover:border-circuit-blue/50 transition-all duration-300 self-start" id="mobile-theme-toggle" aria-label="Toggle theme">
          <i data-feather="sun" class="w-5 h-5 text-circuit-blue block dark:hidden"></i>
          <i data-feather="moon" class="w-5 h-5 text-circuit-blue hidden dark:block"></i>
        </button>
        
        <button class="px-6 py-2 bg-gradient-to-r from-circuit-blue to-circuit-purple text-white font-semibold rounded-lg w-fit">
          Launch Now
        </button>
      </div>
    </div>
  </div>
</nav>

<script>
  // Theme toggle functionality
  function initThemeToggle() {
    const themeToggle = document.getElementById('theme-toggle');
    const mobileThemeToggle = document.getElementById('mobile-theme-toggle');
    
    // Check for saved theme preference or default to dark mode
    const savedTheme = localStorage.getItem('theme') || 'dark';
    
    // Apply theme on page load
    if (savedTheme === 'light') {
      document.documentElement.classList.remove('dark');
    } else {
      document.documentElement.classList.add('dark');
    }
    
    // Toggle theme function
    function toggleTheme() {
      const isDark = document.documentElement.classList.contains('dark');
      
      if (isDark) {
        document.documentElement.classList.remove('dark');
        localStorage.setItem('theme', 'light');
      } else {
        document.documentElement.classList.add('dark');
        localStorage.setItem('theme', 'dark');
      }
      
      // Update feather icons after theme change
      setTimeout(() => {
        if (typeof feather !== 'undefined') {
          feather.replace();
        }
      }, 100);
    }
    
    // Add event listeners
    themeToggle?.addEventListener('click', toggleTheme);
    mobileThemeToggle?.addEventListener('click', toggleTheme);
  }

  // Mobile menu toggle
  const mobileMenuBtn = document.getElementById('mobile-menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');
  
  mobileMenuBtn?.addEventListener('click', () => {
    mobileMenu?.classList.toggle('hidden');
  });

  // Smooth scrolling for navigation links
  document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
      e.preventDefault();
      const target = document.querySelector(this.getAttribute('href'));
      if (target) {
        target.scrollIntoView({
          behavior: 'smooth',
          block: 'start'
        });
      }
    });
  });

  // Initialize theme toggle when DOM is loaded
  document.addEventListener('DOMContentLoaded', initThemeToggle);
</script>
